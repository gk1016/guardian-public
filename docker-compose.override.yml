# Nucbox-specific overrides.
# Caddy removed — engine handles TLS directly.
# No host network mode needed.
services:
  guardian:
    ports:
      - "127.0.0.1:3000:3000"
  guardian-engine:
    ports:
      - "127.0.0.1:3420:3420"
